services:
    postgres:
        image: postgres:15-alpine
        environment:
            POSTGRES_PASSWORD: blogblog
            TZ: America/Sao_Paulo
        ports:
            - '5432:5432'
        volumes:
            - postgres_data:/var/lib/postgresql/data
        networks:
            - BlogNet
    blog-api:
        ports:
            - '5177:8080'
        build:
            dockerfile: blog-api.dockerfile
        depends_on:
            - postgres
        networks:
            - BlogNet

volumes:
    postgres_data:

networks:
    BlogNet:
        driver: bridge
